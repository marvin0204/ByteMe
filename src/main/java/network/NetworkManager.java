
package network;

public class NetworkManager {
    public void sendJoin(String handle, int port) {
        System.out.println("Sende JOIN: " + handle + " " + port);
        // Noch zu implementieren
    }
}
